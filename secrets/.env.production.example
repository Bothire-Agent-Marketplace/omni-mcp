# Production Environment Variables Template
# Copy this file to .env.production.local and fill in real values
# These should be set in Vercel environment variables for deployment

# =============================================================================
# REQUIRED PRODUCTION VARIABLES
# =============================================================================

# Gateway Configuration
GATEWAY_PORT=443
GATEWAY_HOST=0.0.0.0
GATEWAY_URL=https://your-vercel-deployment.vercel.app

# Database Configuration (Production)
# Option 1: Use DATABASE_URL (recommended for Vercel/PostgreSQL providers)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Option 2: Individual database components (if not using DATABASE_URL)
# POSTGRES_HOST=your-db-host.com
# POSTGRES_PORT=5432
# POSTGRES_DB=omni_mcp_production
# POSTGRES_USER=your_production_user
# POSTGRES_PASSWORD=your_secure_password
# POSTGRES_SSL=true

# MCP Server URLs (Production)
LINEAR_SERVER_URL=https://your-linear-mcp-server.vercel.app
QUERYQUILL_SERVER_URL=https://your-queryquill-mcp-server.vercel.app

# Security Configuration (CRITICAL)
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters
MCP_API_KEY=mcp_your-generated-secure-api-key-here
LINEAR_API_KEY=lin_api_your-linear-api-key-here

# Rate Limiting & Security
API_RATE_LIMIT=100
REQUIRE_API_KEY=true
ENABLE_RATE_LIMITING=true
MAX_REQUEST_SIZE=1mb
CORS_CREDENTIALS=false

# Allowed Origins (comma-separated)
ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-admin-panel.com

# Session Configuration
SESSION_TIMEOUT=3600000
MAX_CONCURRENT_SESSIONS=500

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# Logging
LOG_LEVEL=info
APP_TIMEZONE=UTC

# MCP Configuration
MCP_HANDSHAKE_TIMEOUT=10000

# =============================================================================
# VERCEL-SPECIFIC VARIABLES (set automatically by Vercel)
# =============================================================================

# NODE_ENV=production (auto-set)
# VERCEL_ENV=production (auto-set)
# VERCEL_URL=your-deployment.vercel.app (auto-set)

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. Generate secure secrets:
#    - JWT_SECRET: Use a cryptographically secure random string (32+ chars)
#    - MCP_API_KEY: Use the format "mcp_" + 32 random alphanumeric characters
#    
# 2. Database Security:
#    - Always use SSL (POSTGRES_SSL=true or DATABASE_URL with sslmode=require)
#    - Use strong passwords
#    - Limit database access to your Vercel deployment IPs
#
# 3. API Security:
#    - Set restrictive ALLOWED_ORIGINS
#    - Use appropriate rate limits
#    - Enable API key authentication in production
#
# 4. Never commit production secrets to git!
#    - This template should not contain real values
#    - Set these as Vercel environment variables

# =============================================================================
# DEPLOYMENT COMMANDS
# =============================================================================

# 1. Set environment variables in Vercel dashboard:
#    vercel env add JWT_SECRET production
#    vercel env add MCP_API_KEY production
#    vercel env add DATABASE_URL production
#    # ... etc for all variables above
#
# 2. Deploy:
#    vercel --prod
#
# 3. Test deployment:
#    curl -H "x-api-key: your-api-key" https://your-deployment.vercel.app/health 
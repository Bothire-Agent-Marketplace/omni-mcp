# =============================================================================
# MCP Orchestrator Environment Variables Example
# =============================================================================
# Copy this file to .env and update the values as needed
# Note: .env files are ignored by git for security

# Database Configuration
# =============================================================================
POSTGRES_DB=dvdrental
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Docker Network
# =============================================================================
DOCKER_NETWORK=mcp-network

# MCP Server Configuration
# =============================================================================
# Filesystem server allowed directories (absolute paths)
MCP_FILESYSTEM_ALLOWED_DIRS=/Users/yourname/Projects/omni/data/files,/Users/yourname/Projects/omni/data/uploads

# Database tools configuration file path
MCP_DATABASE_TOOLS_CONFIG=/Users/yourname/Projects/omni/compose/configs/database-tools.yaml

# Claude Desktop Configuration Path
# =============================================================================
# macOS: ~/Library/Application Support/Claude/claude_desktop_config.json
# Windows: %APPDATA%\Claude\claude_desktop_config.json
CLAUDE_DESKTOP_CONFIG_PATH=

# Optional: Custom Docker Registry
# =============================================================================
# DOCKER_REGISTRY=your-registry.com
# DOCKER_NAMESPACE=your-namespace

# Optional: Logging and Monitoring
# =============================================================================
# LOG_LEVEL=INFO
# ENABLE_METRICS=false
# METRICS_PORT=9090

# Optional: Security Settings
# =============================================================================
# MCP_FILESYSTEM_READ_ONLY=false
# MCP_DATABASE_READ_ONLY=true
# ALLOWED_ORIGINS=localhost,127.0.0.1 
{
  "name": "dev-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "omni": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "echo 'Dev-tools CLI is ready. Use: pnpm omni --help'",
    "watch:claude-config": "node watch-claude-config.js",
    "docker:build": "node docker-build.js",
    "docker:pull": "node external-docker.js",
    "docker:build:linear": "docker build -f ../servers/linear-mcp-server/Dockerfile -t mcp/linear:latest ..",
    "docker:build:all": "pnpm docker:pull && pnpm docker:build"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "chokidar": "^4.0.3",
    "commander": "^14.0.0",
    "fs-extra": "^11.3.0",
    "js-yaml": "^4.1.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}

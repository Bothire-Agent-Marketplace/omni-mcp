{
  "name": "dev-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "copy:claude-config": "tsx src/claude/copy-config.ts",
    "dev": "echo 'Dev-tools CLI is ready. Use: pnpm omni --help'",
    "docker:build": "node docker-build.js",
    "docker:build:all": "pnpm docker:pull && pnpm docker:build",
    "docker:build:linear": "docker build -f ../servers/linear-mcp-server/Dockerfile -t mcp/linear:latest ..",
    "docker:pull": "node external-docker.js",
    "mcp-bridge": "tsx src/claude/mcp-stdio-bridge.ts",
    "watch:claude-config": "tsx src/claude/config-watcher.ts"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "chokidar": "^4.0.3",
    "commander": "^14.0.0",
    "fs-extra": "^11.3.0",
    "js-yaml": "^4.1.0",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "bin": {
    "omni": "dist/cli/index.js"
  }
}

{
  "_comment": "Reference config for gateway-only setup. Main config is claude_desktop_config.json",
  "mcpServers": {
    "omni-mcp-gateway": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "alpine/socat",
        "STDIO",
        "TCP:localhost:37373"
      ]
    }
  }
}

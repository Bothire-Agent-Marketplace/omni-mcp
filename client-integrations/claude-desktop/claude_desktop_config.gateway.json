{
  "mcpServers": {
    "mcp-gateway": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "--network",
        "mcp-network",
        "alpine/socat",
        "STDIO",
        "TCP:omni-mcp-gateway:3000"
      ]
    }
  }
}
